# Trace every call in every process.
#
#   PYTHON_TRACER_CONFIG=configs/vllm-omni-traceall.yaml PYTHON_TRACER_OUTDIR=traces/vllm-omni-traceall \
#     python -m d3g -- $(which vllm) serve tiny-random/Qwen-Image --omni --port 8000 --enforce-eager
traceall: true
